<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notes App</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* General Reset and Body Styling */
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
      color: #a400b3;
    }
   
    /* Header Styling */
    header {
      background-color: #ffffff;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      z-index: 500;
      position: sticky;
      top: 0;
      padding: 1.5rem 0;
    }

    header a {
      text-decoration: none;
      transition: color 1.5s ease;
    }

    .header-logo {
      font-size: 2.1rem;
      font-weight: bold;
      color: #ff0077c7;
      transition: transform 0.3s ease, color 0.3s ease;

    }

    .header-logo:hover {
      transform: scale(1.1);
      color: #a400b3;
    }

    /* Navbar Links */
    .nav-link {
      color: #495057;
      font-size: 1.4rem;
      font-weight: 650;
      padding-bottom: 15px;
      transition: color 0.3s ease, border-color 0.3s ease; 
    }

    .nav-link:hover,
    .nav-link.active {
      color:#a400b3;
    }

    /* Buttons */
    .btn {
      font-size: 1rem;
      padding: 0.6rem 1.4rem;
      border-radius: 25px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .btn-primary {
      background-color: #ff0090;
      border:seashell;
      color: white;
    }

    .btn-primary:hover {
      background-color:#ff0090;
      border-color: seashell;
    }

    .btn-outline-primary {
      background-color: #ff0090;
      border:seashell;
      color: white;
    
  }

    .btn-outline-primary:hover {
      background-color:#ff0090;
      border-color: seashell;
      
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .nav-link {
        font-size: 1rem;
      }
      .btn {
        font-size: 0.9rem;
        padding: 0.5rem 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <header class="d-flex flex-wrap align-items-center justify-content-between py-3 sticky-top">
      <!-- Logo -->
      <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-decoration-none header-logo">
        NoteNestPro
      </a>

      <!-- Navigation Links -->
      <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
        <li><a href="/" class="nav-link px-3">Home</a></li>
        <li><a href="/feature" class="nav-link px-3">Features</a></li>
        <li><a href="/faqs" class="nav-link px-3">FAQs</a></li>
        <li><a href="/about" class="nav-link px-3">About</a></li>
      </ul>

      <!-- Authentication Buttons -->
      <div class="col-md-3 text-end">
        <a href="/auth/google" class="btn btn-outline-primary me-3">Login</a>
        <a href="/auth/google" class="btn btn-primary">Sign-up</a>
      </div>
    </header>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
